'use strict';

/** SFMC Extension Pack (Expanded) — installs bundled extensions via `extensionPack`; no other runtime behavior. */
function activate() {}

function deactivate() {}

module.exports = { activate, deactivate };
